package com.smartagilify.baseinfo.repository;

import com.smartagilify.baseinfo.entity.BaseInformation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BaseInformationRepository extends JpaRepository<BaseInformation, Long> {
}
